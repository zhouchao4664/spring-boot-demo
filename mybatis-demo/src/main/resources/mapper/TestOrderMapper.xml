<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhouchao.mapper.TestOrderMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.zhouchao.domain.TestOrder">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="order_title" property="orderTitle"/>
        <result column="payment_price" property="paymentPrice"/>
        <result column="payment_date" property="paymentDate"/>
        <result column="total_price" property="totalPrice"/>
        <result column="create_date" property="createDate"/>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
            id, user_id, order_title, payment_price, payment_date, total_price, create_date
        </sql>

</mapper>
