server:
  port: 8881
  jackson:
    serialization:
      write-dates-as-timestamps: true

spring:
  application:
    name: mybatis-demo
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost/test?useUnicode=true&characterEncoding=utf8&useSSL=false&zeroDateTimeBehavior=convertToNull&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: zhouchao

mybatis-plus:
  mapper-locations: classpath:mapper/**/*.xml
  configLocation: classpath:mybatis-config.xml